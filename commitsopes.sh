
# Tomar la ruta actual
ruta=$(pwd)

# Moverse a la ruta del repositorio Git
cd $ruta

# Hacer un git add .
git add .

# Hacer un git commit -m "auto commit"
git commit -m "auto commit"

# Hacer un git push
git push
